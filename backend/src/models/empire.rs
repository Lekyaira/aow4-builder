pub struct Empire {
    pub name: String,
    pub species_form: Option<u32>,
    pub species_traits: Vec<u32>,
    pub culture: Option<u32>,
    pub culture_traits: Vec<u32>,
    pub ruler_type: Option<u32>,
    pub tomes: Vec<u32>,
}
